# Requirements Archive: v1.0 Low-Severity Debt

**Archived:** 2026-04-17
**Status:** ✅ SHIPPED — all 11 v1 requirements delivered

---

## v1 Requirements (All Complete)

### Dead Code Cleanup

- [x] **DEAD-01**: Remove unreachable `break` statements after `return` in every `case` of `chooseWhoWillEvolve()` in `src/app/main-game/roulette-container/roulette-container.component.ts` — ✅ v1.0 (commit 7f68883)
- [x] **DEAD-02**: Remove the double semicolon (`;;`) at `src/app/main-game/roulette-container/roulette-container.component.ts:570` — ✅ v1.0 (commit a39a1ae)

### Naming Correction

- [x] **NAME-01**: Rename `src/app/restart-game-buttom/` directory to `src/app/restart-game-button/`, updating all file names, the class name, the selector, and all import/usage references — ✅ v1.0 (commit 82d32ca)

### Constants & Duplication

- [x] **CONST-01**: Extract `NINCADA_ID = 290` from `roulette-container.component.ts` into `src/app/constants/pokemon-ids.constants.ts` and move Nincada-specific Shedinja evolution logic into `EvolutionService` — ✅ v1.0 (commit fc970b6)
- [x] **CONST-02**: Define the default starting item (Potion) as a single constant in `TrainerService` and reference it from both the field initializer and `resetItems()` — ✅ v1.0 (commit 0e74253)

### Type Safety

- [x] **TYPE-01**: Replace `declare var gtag: any` in `src/app/services/analytics-service/analytics.service.ts` with a typed `GtagCommand` interface covering the GA4 event-tracking API — ✅ v1.0 (commit 8c5b78d)
- [x] **TYPE-02**: Remove `// @ts-ignore` from `end-game.component.ts` and `game-over.component.ts` by providing a full type declaration for `dom-to-image-more` — ✅ v1.0 (commit da7c3f8)

### RxJS Simplification

- [x] **RX-01**: Replace the manual `new Observable` pattern in `src/app/services/item-sprite-service/item-sprite.service.ts` with `of(data)` from RxJS — ✅ v1.0 (commit f1af180)
- [x] **RX-02**: Replace the same manual observable-wrapping pattern in `src/app/services/badges-service/badges.service.ts` with `of(data)` — ✅ v1.0 (commit 9a88823)

### Performance — Lookup Index

- [x] **PERF-01**: Build a persistent reverse index (`Map<variantId, basePokemonId>`) at service construction time in `src/app/services/pokemon-forms-service/pokemon-forms.service.ts` — ✅ v1.0 (commit 77ec2b2)

### Analytics Configuration

- [x] **ANALYTICS-01**: Use a stub/empty GA measurement ID in `src/environments/environment.ts` (dev) so that local development sessions no longer send events to the production Google Analytics property — ✅ v1.0 (commit f717e1b)

---

## Traceability

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| DEAD-01 | Phase 1 | ✅ Complete | Validated — 8 unreachable break; removed |
| DEAD-02 | Phase 1 | ✅ Complete | Validated — ;; removed |
| NAME-01 | Phase 2 | ✅ Complete | Validated — buttom → button across 8 locations |
| CONST-01 | Phase 3 | ✅ Complete | Validated — NINCADA_ID in constants/, Nincada logic in EvolutionService |
| CONST-02 | Phase 3 | ✅ Complete | Validated — DEFAULT_POTION static readonly in TrainerService |
| TYPE-01 | Phase 4 | ✅ Complete | Validated — GtagCommand typed interface |
| TYPE-02 | Phase 4 | ✅ Complete | Validated — full dom-to-image-more .d.ts; @ts-ignore removed |
| RX-01 | Phase 5 | ✅ Complete | Validated — of() in ItemSpriteService |
| RX-02 | Phase 5 | ✅ Complete | Validated — of() in BadgesService |
| PERF-01 | Phase 6 | ✅ Complete | Validated — O(1) Map lookup in PokemonFormsService |
| ANALYTICS-01 | Phase 6 | ✅ Complete | Validated — dev GA ID stubbed to '' |

**v1 requirements delivered: 11/11 ✓**

---
*Requirements archived: 2026-04-17 after v1.0 milestone completion*
