# Requirements Archive — v1.1 Inline Type Matchup Display

**Status:** ✅ All requirements validated and shipped 2026-04-13

## Summary

| Requirement | Phase | Outcome |
|-------------|-------|---------|
| REF-01 | 7 | ✅ Validated — typeAdvantageModal fully removed from gym component |
| REF-02 | 7 | ✅ Validated — typeAdvantageModal fully removed from E4 component |
| SVC-04 | 7 | ✅ Validated — getMatchupTypes() implemented with dedup Sets |
| I18N-01 | 7 | ✅ Validated — strong/weak keys removed from all 6 locales |
| STRIP-01 | 8 | ✅ Validated — strip present in both gym and E4 battle HTML |
| STRIP-02 | 8 | ✅ Validated — column layout: leader icons → label → team icons |
| STRIP-03 | 8 | ✅ Validated — @if (advantageLabel) guard hides label/icons when null |
| STRIP-04 | 8 | ✅ Validated — advantageTypes rendered for advantage/overwhelming |
| STRIP-05 | 8 | ✅ Validated — disadvantageTypes rendered for disadvantage |
| STRIP-06 | 8 | ✅ Validated — live update via teamSubscription → calcVictoryOdds() |

## Original Requirements

### REFACTOR — Remove Type Advantage Modal

**REF-01** — Remove `typeAdvantageModal` from gym battle roulette ✅
Remove `#typeAdvantageModal` ng-template from `gym-battle-roulette.component.html`,
`@ViewChild('typeAdvantageModal')` declaration, `queueTypeAdvantageModal()` method,
and all calls to `queueTypeAdvantageModal()` from `gym-battle-roulette.component.ts`.
The leader presentation modal is kept.

**REF-02** — Remove `typeAdvantageModal` from Elite Four battle roulette ✅
Same removal as REF-01 for `elite-four-battle-roulette.component.ts` and `.html`.

### SVC — Service Extension

**SVC-04** — `TypeMatchupService.getMatchupTypes()` ✅
`getMatchupTypes(team: PokemonItem[], opponentTypes: PokemonType[]): { advantageTypes: PokemonType[], disadvantageTypes: PokemonType[] }`
Implemented with independent dedup Sets; delegates to existing `isStrongAgainst`/`isWeakAgainst` helpers.

### STRIP — Inline Matchup Display

**STRIP-01** — Strip in both battle roulette components ✅
**STRIP-02** — Column layout: leader icons → label → team icons (adjusted from row after visual review) ✅
**STRIP-03** — Null state: leader icon(s) only ✅
**STRIP-04** — Advantage/Overwhelming: show advantageTypes ✅
**STRIP-05** — Disadvantage: show disadvantageTypes ✅
**STRIP-06** — Live update via existing subscription flow ✅

### I18N — Cleanup

**I18N-01** — Remove unused `strong` and `weak` keys from all 6 locale files ✅

## Out of Scope (Confirmed)

- Champion / Rival battle type display
- Animated type icon transitions
- Tooltips on type icons
- `strong`/`weak` label text in strip (icons replace count text)
