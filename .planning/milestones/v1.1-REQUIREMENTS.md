# Requirements Archive: Pokemon Roulette — Milestone v1.1

**Archived:** 2026-04-17
**Milestone:** v1.1 Code Quality & Test Coverage
**Status:** ✅ All 13 requirements delivered

---

## v1.1 Requirements (All Complete)

### Subscription Cleanup

- [x] **SUB-01**: Add `takeUntilDestroyed()` teardown to `restart-game-button.component.ts` — **Delivered in Phase 7**
- [x] **SUB-02**: Add `takeUntilDestroyed()` teardown to `settings-button.component.ts` — **Delivered in Phase 7**

### Internationalization

- [x] **I18N-01**: Replace hardcoded `"Trade!"` with `TranslateService.instant()` call using `game.main.trade.title` key; added to all 6 locale JSON files — **Delivered in Phase 7**

### DOM Access

- [x] **DOM-01**: Replace `document.getElementById('wheel'/'pointer')` with `@ViewChild` template references in `WheelComponent` — **Delivered in Phase 7**

### Defensive Programming

- [x] **BADGE-01**: Add bounds check in `BadgesService.getBadge()` — guards undefined generation/round with `console.warn` — **Delivered in Phase 8**
- [x] **CLEAN-01**: Remove `pokemonIn = pokemonIn` self-assignment no-op from `TrainerService` — **Delivered in Phase 8**

### Performance

- [x] **LOOKUP-01**: Build persistent `Map<number, PokemonItem>` at construction in `PokemonService`; O(1) `getPokemonById()` — **Delivered in Phase 8**

### Component Architecture

- [x] **BATTLE-01**: Extracted `BaseBattleRouletteComponent`; all 4 battle components extend it — 454 lines of duplication eliminated — **Delivered in Phase 9**

### Data Integrity

- [x] **IMMUT-01**: `TrainerService.getTeam()` and `getStored()` return spread copies; `updateTeam()` emits via copy — **Delivered in Phase 8**

### Game State

- [x] **STATE-01**: `GameStateService.initializeStates()` now data-driven via `GENERATION_GAME_CONFIG` + `GenerationService` injection — **Delivered in Phase 8**

### Test Coverage

- [x] **TEST-01**: Battle roulette specs — `calcVictoryOdds`, type-matchup branches, potion flow, multi-leader (gym + E4) — **Delivered in Phase 10**
- [x] **TEST-02**: `RouletteContainerComponent` — all 9 `chooseWhoWillEvolve` branches, `stealPokemon`, `tradePokemon`, `handleRareCandyEvolution` — **Delivered in Phase 10**
- [x] **TEST-03**: `GameStateService` LIFO stack transitions, `PokedexService` shiny edge cases (SHINY-EDGE-01/02/03), `TrainerService` battle forms (pre-existing) — **Delivered in Phase 10**

---

## Traceability

| Requirement | Phase | Status | Notes |
|-------------|-------|--------|-------|
| SUB-01 | 7 | ✅ Complete | `takeUntilDestroyed()` in constructor context |
| SUB-02 | 7 | ✅ Complete | Same pattern as SUB-01 |
| I18N-01 | 7 | ✅ Complete | All 6 locale files updated |
| DOM-01 | 7 | ✅ Complete | `@ViewChild` refs, `id` attrs preserved for CSS |
| BADGE-01 | 8 | ✅ Complete | Guard + `console.warn` + 2 new tests |
| CLEAN-01 | 8 | ✅ Complete | Dead code removed |
| LOOKUP-01 | 8 | ✅ Complete | `readonly` Map, O(1) path |
| BATTLE-01 | 9 | ✅ Complete | 454 LoC eliminated; 2 review fixes applied |
| IMMUT-01 | 8 | ✅ Complete | `addBadge` undefined guard also added |
| STATE-01 | 8 | ✅ Complete | `GENERATION_GAME_CONFIG` covers all 9 gens |
| TEST-01 | 10 | ✅ Complete | 14 new tests (gym × 10, E4 × 4) |
| TEST-02 | 10 | ✅ Complete | 25 new tests |
| TEST-03 | 10 | ✅ Complete | 7 new tests (GameState × 4, Pokedex × 3) |

---

## Future Requirements (Carried Forward)

- Language selector image-based flags — visual UX, needs image assets
- `RouletteContainerComponent` full refactor — tracked, not this milestone
- `GameStateService` desync guard — theoretical, very low probability
- Game state persistence — by design (in-memory acceptable)
- Bulbagarden CDN retry/fallback — by design (acceptable dependency)
- Static data lazy-loading — by design (bundle size acceptable)
- Shiny propagation TODO cleanup — future PR handles it

---

*Requirements archived: 2026-04-17 at milestone v1.1 completion*
