# Plan 03: Fix Hardcoded "Trade!" Title (I18N-01)

**Phase:** 7 — Component Hygiene
**Plan:** 03
**Requirement(s):** I18N-01
**Status:** Pending

## Goal
Replace the hardcoded English string `"Trade!"` assigned to `pkmnTradeTitle` in `RouletteContainerComponent.performTrade()` with the i18n key `"game.main.trade.title"`, and add the corresponding `"title"` key to the `game.main.trade` object in all 6 locale JSON files.

## Files Changed
- `src/app/main-game/roulette-container/roulette-container.component.ts` — change line 563: string literal → i18n key
- `src/assets/i18n/en.json` — add `"title": "Trade!"` to `game.main.trade`
- `src/assets/i18n/es.json` — add `"title": "¡Intercambio!"` to `game.main.trade`
- `src/assets/i18n/fr.json` — add `"title": "Échange\u00a0!"` to `game.main.trade`
- `src/assets/i18n/de.json` — add `"title": "Tausch!"` to `game.main.trade`
- `src/assets/i18n/it.json` — add `"title": "Scambio!"` to `game.main.trade`
- `src/assets/i18n/pt.json` — add `"title": "Troca!"` to `game.main.trade`

## Changes

### `src/app/main-game/roulette-container/roulette-container.component.ts`

**Line 563 — inside `performTrade()`:**
```typescript
// BEFORE:
this.pkmnTradeTitle = "Trade!";

// AFTER:
this.pkmnTradeTitle = "game.main.trade.title";
```

`pkmnTradeTitle` is already used as a translation key elsewhere in the template (passed through `| translate`). This change makes the trade modal title consistent with the rest of the i18n architecture.

---

### Locale files — add `"title"` to `game.main.trade`

All 6 locale files currently have:
```json
"trade": {
    "received": "...",
    "sent": "..."
}
```

Add `"title"` as the first key (before `"received"`):

**`src/assets/i18n/en.json`**
```json
"trade": {
    "title": "Trade!",
    "received": "and received a",
    "sent": "You sent"
}
```

**`src/assets/i18n/es.json`**
```json
"trade": {
    "title": "¡Intercambio!",
    "received": "y recibió un(a)",
    "sent": "Has enviado"
}
```

**`src/assets/i18n/fr.json`**
```json
"trade": {
    "title": "Échange\u00a0!",
    "received": "et reçu un(e)",
    "sent": "Vous avez envoyé"
}
```

**`src/assets/i18n/de.json`**
```json
"trade": {
    "title": "Tausch!",
    "received": "und erhältst",
    "sent": "Du tauschst"
}
```

**`src/assets/i18n/it.json`**
```json
"trade": {
    "title": "Scambio!",
    "received": "e hai ricevuto un",
    "sent": "Hai scambiato"
}
```

**`src/assets/i18n/pt.json`**
```json
"trade": {
    "title": "Troca!",
    "received": "e recebeu ",
    "sent": "Você enviou"
}
```

> Note: The existing `"received"` and `"sent"` values must be preserved exactly as they are — only `"title"` is added.

## Verification
1. Run `ng build` — must produce no errors.
2. All 6 JSON files remain valid JSON. Verify with:
   ```powershell
   @('en','es','fr','de','it','pt') | ForEach-Object {
     $json = Get-Content "src/assets/i18n/$_.json" -Raw | ConvertFrom-Json
     Write-Host "$_: $($json.game.main.trade.title)"
   }
   ```
   Each should print the expected translated title.
3. Grep confirms the key change:
   ```
   grep -n "pkmnTradeTitle" src/app/main-game/roulette-container/roulette-container.component.ts
   ```
   The line that was `"Trade!"` should now read `"game.main.trade.title"`.

## UAT
- [ ] Trigger a trade event in-game (spin wheel to a Pokémon that can be traded)
- [ ] Trade modal opens and displays the correct localized title (e.g., "Trade!" in English)
- [ ] Switch language to Spanish in Settings — trade modal title shows "¡Intercambio!"
- [ ] Switch language to French — trade modal title shows "Échange !"
- [ ] No `[game.main.trade.title]` missing-key placeholder appears in any locale
- [ ] `game.main.trade.received` and `game.main.trade.sent` still display correctly (not broken by edit)
