---
phase: 05-complete-dex-data-region-naming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pokedex/pokedex-by-generation.ts
autonomous: true
requirements:
  - DATA-07
must_haves:
  truths:
    - "File src/app/pokedex/pokedex-by-generation.ts exists and exports pokedexByGeneration"
    - "Gen 1 contains exactly 151 entries (1–151)"
    - "Gen 9 contains exactly 120 entries (906–1025)"
    - "All 9 generations are present with consecutive national-dex ranges"
  artifacts:
    - path: "src/app/pokedex/pokedex-by-generation.ts"
      provides: "Complete national dex ranges for all 9 generations"
      exports: ["pokedexByGeneration"]
  key_links:
    - from: "src/app/pokedex/pokedex-by-generation.ts"
      to: "src/app/trainer-team/pokedex/pokedex.component.ts"
      via: "imported in Plan 05-02"
      pattern: "pokedexByGeneration"
---

<objective>
Create the complete-generation Pokémon data file that replaces the roulette-subset data used by the Local Dex.

Purpose: The current Local Dex imports `pokemonByGeneration` which deliberately excludes legendaries, mythicals, and other special Pokémon from the roulette pool. A separate data file with full consecutive national-dex ranges is needed so the Local Dex can show every Pokémon in the generation without affecting the roulette logic.

Output: `src/app/pokedex/pokedex-by-generation.ts` — a single exported constant with Array.from-generated ranges for all 9 generations.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/main-game/roulette-container/roulettes/pokemon-from-generation-roulette/pokemon-by-generation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pokedex-by-generation.ts with complete national dex ranges</name>
  <files>src/app/pokedex/pokedex-by-generation.ts</files>
  <read_first>
    - src/app/main-game/roulette-container/roulettes/pokemon-from-generation-roulette/pokemon-by-generation.ts
      (read to confirm the Record&lt;number, number[]&gt; type pattern and export style to mirror)
    - src/app/pokedex/ directory listing
      (confirm the directory exists; it already contains pokedex-entry/ subcomponent)
  </read_first>
  <action>
Create the file `src/app/pokedex/pokedex-by-generation.ts` with EXACTLY this content — do not change counts or start indices:

```typescript
/**
 * Complete national Pokédex ranges per generation.
 * Unlike pokemonByGeneration (roulette pool), this includes ALL Pokémon:
 * legendaries, mythicals, Ultra Beasts, Paradox Pokémon, etc.
 *
 * Used by the Local Dex tab to show the full regional pool.
 */
export const pokedexByGeneration: Record<number, number[]> = {
  1: Array.from({ length: 151 }, (_, i) => i + 1),    // 1–151   (Kanto)
  2: Array.from({ length: 100 }, (_, i) => i + 152),   // 152–251 (Johto)
  3: Array.from({ length: 135 }, (_, i) => i + 252),   // 252–386 (Hoenn)
  4: Array.from({ length: 107 }, (_, i) => i + 387),   // 387–493 (Sinnoh)
  5: Array.from({ length: 156 }, (_, i) => i + 494),   // 494–649 (Unova)
  6: Array.from({ length: 72 },  (_, i) => i + 650),   // 650–721 (Kalos)
  7: Array.from({ length: 88 },  (_, i) => i + 722),   // 722–809 (Alola, incl. Ultra Beasts)
  8: Array.from({ length: 96 },  (_, i) => i + 810),   // 810–905 (Galar, incl. Calyrex line)
  9: Array.from({ length: 120 }, (_, i) => i + 906),   // 906–1025 (Paldea, incl. Paradox Pokémon)
};
```

Do NOT inline numeric arrays. Do NOT change any length or start index. This is purely a new file creation — no other files are modified in this task.
  </action>
  <verify>
    <automated>
      # File exists
      test -f src/app/pokedex/pokedex-by-generation.ts && echo "FILE_EXISTS"

      # Export present
      grep -c "export const pokedexByGeneration" src/app/pokedex/pokedex-by-generation.ts

      # All 9 gens present
      grep -c "Array.from" src/app/pokedex/pokedex-by-generation.ts

      # Spot-check Gen 1 length
      grep "length: 151" src/app/pokedex/pokedex-by-generation.ts

      # Spot-check Gen 9 length and start
      grep "length: 120" src/app/pokedex/pokedex-by-generation.ts
      grep "i + 906" src/app/pokedex/pokedex-by-generation.ts

      # TypeScript compiles
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const pokedexByGeneration" src/app/pokedex/pokedex-by-generation.ts` returns 1
    - `grep -c "Array.from" src/app/pokedex/pokedex-by-generation.ts` returns 9
    - `grep "length: 151" src/app/pokedex/pokedex-by-generation.ts` matches (Gen 1)
    - `grep "i + 1\b" src/app/pokedex/pokedex-by-generation.ts` matches (Gen 1 start)
    - `grep "length: 120" src/app/pokedex/pokedex-by-generation.ts` matches (Gen 9)
    - `grep "i + 906" src/app/pokedex/pokedex-by-generation.ts` matches (Gen 9 start)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
    - `ng test --no-watch --browsers=ChromeHeadless` shows 141 specs, 0 failures
  </acceptance_criteria>
  <done>File exists at src/app/pokedex/pokedex-by-generation.ts, exports pokedexByGeneration as Record&lt;number, number[]&gt; with 9 consecutive national-dex ranges using Array.from, TypeScript compiles clean, all tests pass.</done>
</task>

</tasks>

<verification>
After plan execution:
- `grep "export const pokedexByGeneration" src/app/pokedex/pokedex-by-generation.ts` returns a match
- File contains exactly 9 Array.from entries (one per generation)
- `npx tsc --noEmit` exits 0
- `ng test --no-watch --browsers=ChromeHeadless` → 141 specs, 0 failures
</verification>

<success_criteria>
src/app/pokedex/pokedex-by-generation.ts exists, exports pokedexByGeneration with complete consecutive national-dex ranges for all 9 generations (1–151, 152–251, 252–386, 387–493, 494–649, 650–721, 722–809, 810–905, 906–1025), TypeScript strict-mode clean.
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-dex-data-region-naming/05-01-SUMMARY.md`
</output>
