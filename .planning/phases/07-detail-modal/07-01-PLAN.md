---
phase: 07-detail-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/i18n/en.json
  - src/assets/i18n/es.json
  - src/assets/i18n/fr.json
  - src/assets/i18n/pt.json
  - src/assets/i18n/de.json
  - src/assets/i18n/it.json
  - src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts
  - src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.html
  - src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.css
  - src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.spec.ts
autonomous: true
requirements: [DETAIL-01, DETAIL-02, SHINY-02]

must_haves:
  truths:
    - "PokedexDetailModalComponent renders official artwork in a 160×160px circle"
    - "Modal header shows zero-padded Pokédex number (#025) and localized Pokémon name"
    - "Shiny toggle button only renders when entry.shiny === true"
    - "Toggling shiny switches artworkUrl between regular and shiny official-artwork paths"
    - "Alternate form buttons only render when getFormIds(pokemonId).length > 1"
    - "Selecting a form button updates selectedFormId and thus artworkUrl"
    - "detail.shiny.on / detail.shiny.off keys exist in all 6 locale files"
  artifacts:
    - path: "src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts"
      provides: "Modal component class with artworkUrl, pokemonNameKey, hasShinyToggle, hasAlternateForms, alternateForms computed getters"
      exports: ["PokedexDetailModalComponent"]
    - path: "src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.html"
      provides: "Modal template: header with name/number, artwork circle, shiny toggle, form buttons"
    - path: "src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.spec.ts"
      provides: "Unit tests covering DETAIL-01, DETAIL-02, SHINY-02 component behaviours"
  key_links:
    - from: "pokedex-detail-modal.component.ts"
      to: "NgbActiveModal"
      via: "constructor injection, activeModal.dismiss()"
      pattern: "activeModal\\.dismiss"
    - from: "pokedex-detail-modal.component.ts"
      to: "PokemonFormsService.getFormIds()"
      via: "hasAlternateForms getter"
      pattern: "getFormIds\\(this\\.pokemonId\\)\\.length"
    - from: "pokedex-detail-modal.component.html"
      to: "artworkUrl getter"
      via: "[src]='artworkUrl' binding"
      pattern: "\\[src\\]=\"artworkUrl\""
---

<objective>
Create `PokedexDetailModalComponent` — the rich Pokémon detail modal that will be opened by `PokedexComponent` in Plan 02. Also seeds all 6 locale files with the `detail.shiny.on` / `detail.shiny.off` i18n keys the modal template requires.

Purpose: DETAIL-01 (circle artwork + number + name), DETAIL-02 (alternate forms), SHINY-02 (shiny toggle).
Output: New component at `src/app/pokedex/pokedex-detail-modal/`, updated i18n files, passing spec.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-detail-modal/07-RESEARCH.md

@src/app/services/pokedex-service/pokedex.service.ts
@src/app/services/pokemon-forms-service/pokemon-forms.service.ts
@src/app/interfaces/pokemon-form.ts
@src/app/interfaces/pokemon-item.ts
@src/app/services/dark-mode-service/dark-mode.service.ts
@src/app/pokedex/pokedex-entry/pokedex-entry.component.ts
</context>

<interfaces>
<!-- Key contracts the executor needs — extracted from codebase -->

From src/app/services/pokedex-service/pokedex.service.ts:
```typescript
export interface PokedexEntry {
  won: boolean;
  sprite: string | null;
  shiny?: boolean;
}
```

From src/app/interfaces/pokemon-item.ts:
```typescript
export interface PokemonItem extends WheelItem {
  pokemonId: number;
  sprite: { front_default: string; front_shiny: string; } | null;
  shiny: boolean;
  power: 1 | 2 | 3 | 4 | 5;
}
// WheelItem: { text: string; fillStyle: string; weight: number; }
```

From src/app/interfaces/pokemon-form.ts:
```typescript
export interface PokemonForm extends WheelItem {
  pokemonId: number;
}
// text property (i18n key like 'pokemon.rattata-alola') inherited from WheelItem
```

From src/app/services/pokemon-forms-service/pokemon-forms.service.ts:
```typescript
hasForms(pokemon: PokemonItem): boolean           // ← requires PokemonItem, NOT id
getFormIds(pokemonId: number): number[]           // ← returns [] for single-form Pokémon
getPokemonForms(pokemon: PokemonItem): PokemonForm[]  // ← requires PokemonItem
```

From src/app/services/pokemon-service/pokemon.service.ts:
```typescript
getPokemonById(pokemonId: number): PokemonItem | undefined
// Returns undefined for alt-form IDs (>10000) — never call for alt-form display names
nationalDexPokemon: PokemonItem[]
```

From src/app/services/dark-mode-service/dark-mode.service.ts:
```typescript
darkMode$: Observable<boolean>
```

NgbActiveModal (from @ng-bootstrap/ng-bootstrap):
```typescript
// Injected into modal component
activeModal.dismiss(reason?: any): void
activeModal.close(result?: any): void
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detail.shiny i18n keys to all 6 locale files</name>
  <files>
    src/assets/i18n/en.json,
    src/assets/i18n/es.json,
    src/assets/i18n/fr.json,
    src/assets/i18n/pt.json,
    src/assets/i18n/de.json,
    src/assets/i18n/it.json
  </files>
  <read_first>
    - src/assets/i18n/en.json (read full file — add keys at root level alongside "pokedex" key)
    - src/assets/i18n/es.json
    - src/assets/i18n/fr.json
    - src/assets/i18n/pt.json
    - src/assets/i18n/de.json
    - src/assets/i18n/it.json
  </read_first>
  <action>
    Each locale file currently ends with:
    ```json
      "pokedex": { ... }
    }
    ```

    Add a new `"detail"` key at the root level, as a sibling of `"pokedex"`. Insert it BEFORE the closing `}` of the root object, after the `"pokedex"` block (add a comma after `"pokedex": { ... }` and then the new key).

    Exact values to add per locale:

    **en.json:**
    ```json
    "detail": {
      "shiny": {
        "on": "✨ Shiny",
        "off": "Normal"
      }
    }
    ```

    **es.json:**
    ```json
    "detail": {
      "shiny": {
        "on": "✨ Shiny",
        "off": "Normal"
      }
    }
    ```

    **fr.json:**
    ```json
    "detail": {
      "shiny": {
        "on": "✨ Chromatique",
        "off": "Normal"
      }
    }
    ```

    **pt.json:**
    ```json
    "detail": {
      "shiny": {
        "on": "✨ Shiny",
        "off": "Normal"
      }
    }
    ```

    **de.json:**
    ```json
    "detail": {
      "shiny": {
        "on": "✨ Shiny",
        "off": "Normal"
      }
    }
    ```

    **it.json:**
    ```json
    "detail": {
      "shiny": {
        "on": "✨ Shiny",
        "off": "Normale"
      }
    }
    ```

    Ensure the JSON remains valid after editing (no trailing commas, correct nesting).
  </action>
  <verify>
    <automated>
      node -e "['en','es','fr','pt','de','it'].forEach(l => { const j = JSON.parse(require('fs').readFileSync('src/assets/i18n/' + l + '.json','utf8')); if (!j.detail?.shiny?.on || !j.detail?.shiny?.off) throw new Error(l + ' missing detail.shiny keys'); console.log(l + ': OK'); })"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep '"detail"' src/assets/i18n/en.json` outputs at least one match
    - `grep '"on"' src/assets/i18n/en.json` outputs a line containing `✨`
    - `grep '"off"' src/assets/i18n/fr.json` outputs `"off": "Normal"`
    - `grep '"on"' src/assets/i18n/fr.json` outputs `"on": "✨ Chromatique"`
    - All 6 locale files parse as valid JSON (node command above exits 0)
  </acceptance_criteria>
  <done>All 6 locale files contain a valid `detail.shiny.on` and `detail.shiny.off` key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PokedexDetailModalComponent (ts + html + css + spec)</name>
  <files>
    src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts,
    src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.html,
    src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.css,
    src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.spec.ts
  </files>
  <read_first>
    - src/app/pokedex/pokedex-entry/pokedex-entry.component.ts (conventions: standalone, single quotes, 2-space indent)
    - src/app/trainer-team/pokedex/pokedex.component.ts (NgbModal usage pattern)
    - src/app/main-game/end-game/end-game.component.html (circle image pattern with rounded-circle)
    - src/app/services/pokemon-forms-service/pokemon-forms.service.ts (getFormIds / getPokemonForms API)
    - .planning/phases/07-detail-modal/07-RESEARCH.md (full component skeleton and template HTML)
  </read_first>
  <behavior>
    Tests that must be written in the spec file BEFORE implementing the component:

    - `should create` — component initializes with pokemonId=25 and a seen entry
    - `artworkUrl returns regular URL when showShiny is false` — expect `artworkUrl` to equal `'https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/25.png'`
    - `artworkUrl returns shiny URL when showShiny is true` — expect `artworkUrl` to equal `'https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/shiny/25.png'`
    - `artworkUrl uses selectedFormId when a form is selected` — set `selectedFormId = 10001`, expect URL to contain `10001`
    - `formatPokemonNumber pads IDs under 1000` — `formatPokemonNumber(25)` → `'#025'`
    - `formatPokemonNumber does not pad IDs 1000+` — `formatPokemonNumber(1011)` → `'#1011'`
    - `hasShinyToggle is false when entry.shiny is undefined` — entry `{ won: false, sprite: null }` → `hasShinyToggle` is false
    - `hasShinyToggle is true when entry.shiny === true` — entry `{ won: false, sprite: null, shiny: true }` → `hasShinyToggle` is true
    - `template does NOT render shiny toggle when entry.shiny is falsy` — fixture query for `.btn-outline-warning` or `[data-testid="shiny-toggle"]` returns null
    - `template renders shiny toggle button when entry.shiny is true` — fixture query for the shiny toggle button returns non-null
    - `hasAlternateForms is true when getFormIds returns array length > 1` — mock `getFormIds(386)` → `[386, 10001, 10002, 10003]`; expect `hasAlternateForms` to be true
    - `hasAlternateForms is false when getFormIds returns empty array` — mock `getFormIds(1)` → `[]`; expect false
    - `alternateForms returns empty array when getPokemonById returns undefined` — mock `getPokemonById` to return undefined; expect `alternateForms` to equal `[]`
  </behavior>
  <action>
    **WRITE THE SPEC FILE FIRST (RED phase), then implement to make tests pass (GREEN phase).**

    ---

    ### spec file: `pokedex-detail-modal.component.spec.ts`

    Use Jasmine + TestBed. Provide spy objects for `DarkModeService` (darkMode$: of(false)), `PokemonService` (getPokemonById returns a PokemonItem with pokemonId=25, text='pokemon.pikachu'), `PokemonFormsService` (getFormIds returns [], getPokemonForms returns []). Import `TranslateModule.forRoot()` and `NgbActiveModal` from `@ng-bootstrap/ng-bootstrap`. Provide a mock `NgbActiveModal` (jasmine.createSpyObj with `dismiss` and `close`).

    Run tests after writing the spec to confirm RED (they fail because the component file doesn't exist yet or the methods are missing), then implement.

    ---

    ### TypeScript: `pokedex-detail-modal.component.ts`

    ```typescript
    import { Component, Input, OnInit } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';
    import { TranslatePipe } from '@ngx-translate/core';
    import { Observable } from 'rxjs';

    import { DarkModeService } from '../../services/dark-mode-service/dark-mode.service';
    import { PokemonService } from '../../services/pokemon-service/pokemon.service';
    import { PokemonFormsService } from '../../services/pokemon-forms-service/pokemon-forms.service';
    import { PokedexEntry } from '../../services/pokedex-service/pokedex.service';
    import { PokemonForm } from '../../interfaces/pokemon-form';

    @Component({
      selector: 'app-pokedex-detail-modal',
      standalone: true,
      imports: [CommonModule, TranslatePipe],
      templateUrl: './pokedex-detail-modal.component.html',
      styleUrl: './pokedex-detail-modal.component.css'
    })
    export class PokedexDetailModalComponent implements OnInit {
      @Input() pokemonId!: number;
      @Input() entry!: PokedexEntry;

      showShiny = false;
      selectedFormId!: number;
      darkMode!: Observable<boolean>;
      hasError = false;

      readonly fallbackUrl = 'https://raw.githubusercontent.com/PokeAPI/sprites/refs/heads/master/sprites/items/unknown.png';

      constructor(
        public activeModal: NgbActiveModal,
        private pokemonService: PokemonService,
        private pokemonFormsService: PokemonFormsService,
        private darkModeService: DarkModeService
      ) {}

      ngOnInit(): void {
        this.selectedFormId = this.pokemonId;
        this.darkMode = this.darkModeService.darkMode$;
      }

      get artworkUrl(): string {
        const base = 'https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork';
        return this.showShiny
          ? `${base}/shiny/${this.selectedFormId}.png`
          : `${base}/${this.selectedFormId}.png`;
      }

      get displayUrl(): string {
        return this.hasError ? this.fallbackUrl : this.artworkUrl;
      }

      get pokemonNameKey(): string {
        if (this.selectedFormId === this.pokemonId) {
          return this.pokemonService.getPokemonById(this.pokemonId)?.text ?? 'pokemon.unknown';
        }
        const form = this.alternateForms.find(f => f.pokemonId === this.selectedFormId);
        return form?.text ?? 'pokemon.unknown';
      }

      get hasShinyToggle(): boolean {
        return this.entry?.shiny === true;
      }

      get hasAlternateForms(): boolean {
        return this.pokemonFormsService.getFormIds(this.pokemonId).length > 1;
      }

      get alternateForms(): PokemonForm[] {
        const base = this.pokemonService.getPokemonById(this.pokemonId);
        if (!base) return [];
        return this.pokemonFormsService.getPokemonForms(base);
      }

      formatPokemonNumber(id: number): string {
        if (id >= 1000) return `#${id}`;
        return `#${id.toString().padStart(3, '0')}`;
      }

      onArtworkError(): void {
        this.hasError = true;
      }
    }
    ```

    ⚠️ Do NOT call `hasForms(pokemon: PokemonItem)` — it requires a full PokemonItem. Use `getFormIds(this.pokemonId).length > 1` directly (pitfall from RESEARCH.md).
    ⚠️ Do NOT call `getPokemonById(formId)` for alt-form IDs (e.g., 10001, 10091) — returns undefined. Use `PokemonForm.text` from `alternateForms` getter instead.

    ---

    ### HTML: `pokedex-detail-modal.component.html`

    ```html
    <div class="modal-header" [ngClass]="(darkMode | async) ? 'bg-dark text-white' : ''">
      <h5 class="modal-title">{{ pokemonNameKey | translate }}</h5>
      <button type="button" class="btn-close"
        [ngClass]="(darkMode | async) ? 'btn-close-white' : ''"
        (click)="activeModal.dismiss()" aria-label="Close"></button>
    </div>
    <div class="modal-body text-center" [ngClass]="(darkMode | async) ? 'bg-dark text-white' : ''">

      <!-- Official artwork circle -->
      <div class="pokemon-artwork-circle rounded-circle mx-auto mb-3"
           [ngClass]="(darkMode | async) ? 'black-border shadow' : 'border'">
        <img [src]="displayUrl"
             loading="lazy"
             class="img-fluid rounded-circle"
             [alt]="pokemonNameKey | translate"
             (error)="onArtworkError()">
      </div>

      <!-- Pokédex number -->
      <p class="fw-bold mb-1">{{ formatPokemonNumber(pokemonId) }}</p>

      <!-- Shiny toggle (SHINY-02: only render when entry.shiny === true) -->
      @if (hasShinyToggle) {
        <button class="btn btn-sm mb-2"
                [ngClass]="showShiny ? 'btn-warning' : 'btn-outline-warning'"
                (click)="showShiny = !showShiny">
          {{ showShiny ? ('detail.shiny.on' | translate) : ('detail.shiny.off' | translate) }}
        </button>
      }

      <!-- Alternate form tabs (DETAIL-02: only render when hasAlternateForms) -->
      @if (hasAlternateForms) {
        <div class="d-flex flex-wrap gap-2 justify-content-center mt-2">
          @for (form of alternateForms; track form.pokemonId) {
            <button class="btn btn-sm"
                    [ngClass]="selectedFormId === form.pokemonId ? 'btn-primary' : 'btn-outline-secondary'"
                    (click)="selectedFormId = form.pokemonId">
              {{ form.text | translate }}
            </button>
          }
        </div>
      }

    </div>
    ```

    Note: The `(error)="onArtworkError()"` handler sets `hasError = true`, and the `displayUrl` getter returns `fallbackUrl` when `hasError` is true. The template binds `[src]="displayUrl"` so Angular re-evaluates to the fallback after `hasError` is set.

    ---

    ### CSS: `pokedex-detail-modal.component.css`

    ```css
    .pokemon-artwork-circle {
      width: 160px;
      height: 160px;
      aspect-ratio: 1;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    ```

    That is the full CSS needed. The `rounded-circle` Bootstrap class handles `border-radius: 50%`. The `border` or `black-border shadow` classes come from dark-mode NgClass binding.
  </action>
  <verify>
    <automated>ng test --include='**/pokedex-detail-modal**' --watch=false</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/app/pokedex/pokedex-detail-modal/` lists all 4 files (ts, html, css, spec.ts)
    - `grep "PokedexDetailModalComponent" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts` matches
    - `grep "export class PokedexDetailModalComponent" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts` matches
    - `grep "@Input() pokemonId" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts` matches
    - `grep "@Input() entry" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts` matches
    - `grep "hasShinyToggle" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts` matches
    - `grep "hasAlternateForms" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts` matches
    - `grep "official-artwork" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.ts` matches (artworkUrl getter)
    - `grep "detail.shiny.on" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.html` matches
    - `grep "pokemon-artwork-circle" src/app/pokedex/pokedex-detail-modal/pokedex-detail-modal.component.css` matches
    - `ng test --include='**/pokedex-detail-modal**' --watch=false` exits 0 with 0 failures
    - Spec file contains at least 10 `it(` blocks
  </acceptance_criteria>
  <done>
    `PokedexDetailModalComponent` passes all spec tests. Component has all required getters (artworkUrl, pokemonNameKey, hasShinyToggle, hasAlternateForms, alternateForms, formatPokemonNumber). Template renders artwork circle, number, conditional shiny toggle, conditional form tabs. All 6 locale files have `detail.shiny.on` / `detail.shiny.off` keys.
  </done>
</task>

</tasks>

<verification>
Run full pokedex suite:
```
ng test --include='**/pokedex**' --watch=false
```
Expected: 0 failures. New modal spec contributes ≥10 tests.

Validate locale JSON:
```
node -e "['en','es','fr','pt','de','it'].forEach(l => JSON.parse(require('fs').readFileSync('src/assets/i18n/' + l + '.json','utf8')))"
```
Must exit 0 (all files are valid JSON).
</verification>

<success_criteria>
- `src/app/pokedex/pokedex-detail-modal/` directory contains all 4 component files
- `PokedexDetailModalComponent` exports from `pokedex-detail-modal.component.ts`
- All 6 locale files contain `detail.shiny.on` and `detail.shiny.off` as nested JSON keys
- `ng test --include='**/pokedex-detail-modal**' --watch=false` exits 0 with ≥10 passing specs and 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-detail-modal/07-01-SUMMARY.md`
</output>
