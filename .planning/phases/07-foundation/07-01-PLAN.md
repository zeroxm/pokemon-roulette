---
phase: 7
plan: 07-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts
  - src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.html
  - src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts
  - src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.html
  - src/assets/i18n/en.json
  - src/assets/i18n/de.json
  - src/assets/i18n/es.json
  - src/assets/i18n/fr.json
  - src/assets/i18n/it.json
  - src/assets/i18n/pt.json
autonomous: true
requirements: [REF-01, REF-02, I18N-01]

must_haves:
  truths:
    - "No `typeAdvantageModal` ViewChild, method, or template exists in either battle component"
    - "The gymLeaderPresentationModal and eliteFourPresentationModal modals are untouched and still functional"
    - "The `strong` and `weak` keys are absent from all 6 locale files; `overwhelming`, `advantage`, `disadvantage` remain"
    - "Both components compile cleanly — no TypeScript errors introduced by the removal"
  artifacts:
    - path: "src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts"
      provides: "GymBattleRouletteComponent without typeAdvantageModal"
    - path: "src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.html"
      provides: "Gym HTML without #typeAdvantageModal ng-template"
    - path: "src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts"
      provides: "EliteFourBattleRouletteComponent without typeAdvantageModal"
    - path: "src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.html"
      provides: "Elite Four HTML without #typeAdvantageModal ng-template"
    - path: "src/assets/i18n/*.json"
      provides: "All 6 locale files with 3-key typeAdvantage objects (no strong/weak)"
  key_links:
    - from: "gymLeaderPresentationModal ViewChild"
      to: "#gymLeaderPresentationModal template in HTML"
      via: "TemplateRef<any> — must remain intact"
    - from: "itemUsedModal ViewChild"
      to: "#itemUsedModal template in HTML"
      via: "TemplateRef<any> — must remain intact in both components"
    - from: "disadvantage JSON value"
      to: "closing brace of typeAdvantage object"
      via: "must have NO trailing comma after strong/weak removal"
---

<objective>
Remove the `typeAdvantageModal` from both battle components and strip the orphaned `strong`/`weak` i18n keys from all 6 locale files.

Purpose: The type advantage modal is being replaced by an inline strip (Phase 8). Phase 7 clears the dead code before the strip is built.
Output: Both battle component TS + HTML files free of typeAdvantageModal; all 6 locale files with exactly 3 typeAdvantage keys.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove typeAdvantageModal from GymBattleRouletteComponent (REF-01)</name>

  <read_first>
    - src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts
    - src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.html
  </read_first>

  <files>
    src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts
    src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.html
  </files>

  <action>
Make the following deletions. Do NOT remove anything else. Read each file first to confirm line numbers before editing.

**GYM .TS — 5 deletions:**

1. Remove the `typeAdvantageModal` ViewChild declaration (line ~49). Delete this entire line:
   ```
   @ViewChild('typeAdvantageModal', { static: true }) typeAdvantageModal!: TemplateRef<any>;
   ```
   Keep the lines above and below (`gymLeaderPresentationModal` and `itemUsedModal` ViewChild declarations stay).

2. Remove the PC-swap re-show block from inside the `teamSubscription` callback (lines ~93–95). Delete these 3 lines (the comment + the if block):
   ```
       // BATTLE-04: re-show modal on PC swap
       if (this.currentGameState === 'gym-battle' && this.currentLeader && this.advantageLabel) {
         this.queueTypeAdvantageModal();
       }
   ```
   The surrounding code remains: `this.trainerTeam = team;` above, `this.calcVictoryOdds();` above the deleted block.

3. Remove the modal call from the `gameSubscription` handler (line ~108). Delete this one line:
   ```
       this.queueTypeAdvantageModal();
   ```
   Context: it appears immediately after `this.modalQueueService.open(this.gymLeaderPresentationModal, { centered: true, size: 'lg' });` closes. The `});` closing the `if (state === 'gym-battle')` block remains.

4. Remove the `queueTypeAdvantageModal()` call from inside `getCurrentLeader()`'s `translate.get().subscribe()` callback (line ~237). Delete this one line:
   ```
       this.queueTypeAdvantageModal();
   ```
   Context: it appears after `this.calcVictoryOdds();` inside the `translate.get(this.currentLeader.name).pipe(take(1)).subscribe(translated => { ... })` callback. `this.calcVictoryOdds();` immediately before it stays.

5. Remove the entire `queueTypeAdvantageModal()` private method (lines ~194–198). Delete all 5 lines:
   ```
     private queueTypeAdvantageModal(): void {
       if (this.advantageLabel) {
         this.modalQueueService.open(this.typeAdvantageModal, { centered: true, size: 'md' });
       }
     }
   ```

**What to keep in gym .ts:**
- `TemplateRef` in the import statement — still needed by `gymLeaderPresentationModal` and `itemUsedModal` ViewChildren.
- `ViewChild` in the import — still needed by the remaining two @ViewChild declarations.
- `ModalQueueService` import and injection — still used for gymLeaderPresentationModal and itemUsedModal.
- All of: `advantageLabel`, `advantageLabelKey`, `strongCount`, `weakCount` fields — Phase 8 uses them for the inline strip.
- `getTypeIconUrl()` method — Phase 8 uses it.
- The entire `calcVictoryOdds()` method and `plusModifiers()`, `getCurrentLeader()`, etc.

**GYM .HTML — 1 deletion:**

Remove the entire `<ng-template #typeAdvantageModal let-modal>` block. Delete lines ~40–56 (inclusive):
```html
<ng-template #typeAdvantageModal let-modal>
    <div class="modal-body">
        <h2>{{ advantageLabelKey | translate }}</h2>
        <div class="type-icons">
            @for (type of currentLeader.types; track $index) {
                <img [src]="getTypeIconUrl(type)" [alt]="type" height="32">
            }
        </div>
        <p>{{ 'game.main.roulette.gym.typeAdvantage.strong' | translate }}: {{ strongCount }}</p>
        <p>{{ 'game.main.roulette.gym.typeAdvantage.weak' | translate }}: {{ weakCount }}</p>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-outline-primary" (click)="closeModal()">
            {{ 'game.main.roulette.gym.go' | translate }}
        </button>
    </div>
</ng-template>
```
The `#gymLeaderPresentationModal` and `#itemUsedModal` ng-templates above it stay untouched. The file ends after the closing `</ng-template>` of `#itemUsedModal`.
  </action>

  <acceptance_criteria>
Run each of these grep commands — ALL must return ZERO matches:

```bash
# No typeAdvantageModal ViewChild
grep -n "typeAdvantageModal" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts

# No queueTypeAdvantageModal method or calls
grep -n "queueTypeAdvantageModal" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts

# No typeAdvantageModal template in HTML
grep -n "typeAdvantageModal" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.html
```

Run these grep commands — ALL must return matches (confirming kept code is intact):

```bash
# gymLeaderPresentationModal still present in TS
grep -n "gymLeaderPresentationModal" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts

# itemUsedModal still present in TS
grep -n "itemUsedModal" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts

# TemplateRef still in import
grep -n "TemplateRef" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts

# ModalQueueService still injected
grep -n "ModalQueueService" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts

# advantageLabel field still present (Phase 8 uses it)
grep -n "advantageLabel" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.ts

# gymLeaderPresentationModal template still in HTML
grep -n "gymLeaderPresentationModal" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.html

# itemUsedModal template still in HTML
grep -n "itemUsedModal" src/app/main-game/roulette-container/roulettes/gym-battle-roulette/gym-battle-roulette.component.html
```
  </acceptance_criteria>

  <done>
`gym-battle-roulette.component.ts` contains no references to `typeAdvantageModal` or `queueTypeAdvantageModal`. The HTML contains no `#typeAdvantageModal` ng-template. The `gymLeaderPresentationModal`, `itemUsedModal`, `ModalQueueService`, `TemplateRef`, `advantageLabel`, and `getTypeIconUrl` are all intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove typeAdvantageModal from EliteFourBattleRouletteComponent (REF-02)</name>

  <read_first>
    - src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts
    - src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.html
  </read_first>

  <files>
    src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts
    src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.html
  </files>

  <action>
Mirror the same removals as Task 1 but for the Elite Four component. Read each file first to confirm line numbers.

**ELITE FOUR .TS — 5 deletions:**

1. Remove the `typeAdvantageModal` ViewChild declaration (line ~49). Delete this entire line:
   ```
   @ViewChild('typeAdvantageModal', { static: true }) typeAdvantageModal!: TemplateRef<any>;
   ```
   Keep `eliteFourPresentationModal` and `itemUsedModal` ViewChild declarations.

2. Remove the PC-swap re-show block from inside the `teamSubscription` callback (lines ~84–86). Delete these 3 lines:
   ```
       if (this.currentGameState === 'elite-four-battle' && this.currentElite && this.advantageLabel) {
         this.queueTypeAdvantageModal();
       }
   ```
   The surrounding code remains: `this.trainerTeam = team;` above, `this.calcVictoryOdds();` above the deleted block.

3. Remove the modal call from the `gameSubscription` handler (line ~99). Delete this one line:
   ```
       this.queueTypeAdvantageModal();
   ```
   Context: appears immediately after `this.modalQueueService.open(this.eliteFourPresentationModal, { centered: true, size: 'lg' });` and before the closing `}` of the `if (state === 'elite-four-battle')` block.

4. Remove the `queueTypeAdvantageModal()` call from inside `getCurrentElite()`'s subscribe callback (line ~219). Delete this one line:
   ```
       this.queueTypeAdvantageModal();
   ```
   Context: appears after `this.calcVictoryOdds();` inside the `translate.get(this.currentElite.name).pipe(take(1)).subscribe(translated => { ... })` callback.

5. Remove the entire `queueTypeAdvantageModal()` private method (lines ~261–264). Delete all 5 lines:
   ```
     private queueTypeAdvantageModal(): void {
       if (this.advantageLabel) {
         this.modalQueueService.open(this.typeAdvantageModal, { centered: true, size: 'md' });
       }
     }
   ```

**What to keep in elite-four .ts:**
- `TemplateRef` in the import — still needed by `eliteFourPresentationModal` and `itemUsedModal`.
- `ViewChild` in the import — still needed by the remaining two @ViewChild declarations.
- `ModalQueueService` import and injection — still used for eliteFourPresentationModal and itemUsedModal.
- All of: `advantageLabel`, `advantageLabelKey`, `strongCount`, `weakCount` fields.
- `getTypeIconUrl()` method.
- The entire `calcVictoryOdds()`, `getCurrentElite()`, etc.

**ELITE FOUR .HTML — 1 deletion:**

Remove the entire `<ng-template #typeAdvantageModal let-modal>` block. Delete lines ~43–59 (inclusive):
```html
<ng-template #typeAdvantageModal let-modal>
    <div class="modal-body">
        <h2>{{ advantageLabelKey | translate }}</h2>
        <div class="type-icons">
            @for (type of currentElite.types; track $index) {
                <img [src]="getTypeIconUrl(type)" [alt]="type" height="32">
            }
        </div>
        <p>{{ 'game.main.roulette.gym.typeAdvantage.strong' | translate }}: {{ strongCount }}</p>
        <p>{{ 'game.main.roulette.gym.typeAdvantage.weak' | translate }}: {{ weakCount }}</p>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-outline-primary" (click)="closeModal()">
            {{ 'game.main.roulette.gym.go' | translate }}
        </button>
    </div>
</ng-template>
```
The `#eliteFourPresentationModal` and `#itemUsedModal` ng-templates stay untouched.
  </action>

  <acceptance_criteria>
Run each of these grep commands — ALL must return ZERO matches:

```bash
# No typeAdvantageModal ViewChild
grep -n "typeAdvantageModal" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts

# No queueTypeAdvantageModal method or calls
grep -n "queueTypeAdvantageModal" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts

# No typeAdvantageModal template in HTML
grep -n "typeAdvantageModal" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.html
```

Run these grep commands — ALL must return matches:

```bash
# eliteFourPresentationModal still present in TS
grep -n "eliteFourPresentationModal" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts

# itemUsedModal still present in TS
grep -n "itemUsedModal" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts

# TemplateRef still in import
grep -n "TemplateRef" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts

# ModalQueueService still injected
grep -n "ModalQueueService" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts

# advantageLabel field still present
grep -n "advantageLabel" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.ts

# eliteFourPresentationModal template still in HTML
grep -n "eliteFourPresentationModal" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.html

# itemUsedModal template still in HTML
grep -n "itemUsedModal" src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/elite-four-battle-roulette.component.html
```
  </acceptance_criteria>

  <done>
`elite-four-battle-roulette.component.ts` contains no references to `typeAdvantageModal` or `queueTypeAdvantageModal`. The HTML contains no `#typeAdvantageModal` ng-template. The `eliteFourPresentationModal`, `itemUsedModal`, `ModalQueueService`, `TemplateRef`, `advantageLabel`, and `getTypeIconUrl` are all intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove `strong` and `weak` i18n keys from all 6 locale files (I18N-01)</name>

  <read_first>
    - src/assets/i18n/en.json
    - src/assets/i18n/de.json
    - src/assets/i18n/es.json
    - src/assets/i18n/fr.json
    - src/assets/i18n/it.json
    - src/assets/i18n/pt.json
  </read_first>

  <files>
    src/assets/i18n/en.json
    src/assets/i18n/de.json
    src/assets/i18n/es.json
    src/assets/i18n/fr.json
    src/assets/i18n/it.json
    src/assets/i18n/pt.json
  </files>

  <action>
In every one of the 6 locale files, locate the `"typeAdvantage"` object (found at JSON path `game.main.roulette.gym.typeAdvantage` — search for `"typeAdvantage":` to find it). It currently has 5 keys: `overwhelming`, `advantage`, `disadvantage`, `strong`, `weak`. Remove `strong` and `weak` so that only 3 keys remain.

**Current structure in all 6 files (key values differ per locale):**
```json
"typeAdvantage": {
    "overwhelming": "...",
    "advantage": "...",
    "disadvantage": "...",
    "strong": "...",
    "weak": "..."
}
```

**Target structure after edit:**
```json
"typeAdvantage": {
    "overwhelming": "...",
    "advantage": "...",
    "disadvantage": "..."
}
```

**CRITICAL JSON rule:** After removing `"strong"` and `"weak"`, the `"disadvantage"` line becomes the last key in the object. Its value must NOT have a trailing comma. Before editing, `"disadvantage": "...",` has a comma because `"strong"` followed it. After removal, that line must become `"disadvantage": "..."` (no comma).

**Per-locale values to remove (delete these exact lines from each file):**

| Locale | Line to delete — strong | Line to delete — weak |
|--------|-------------------------|-----------------------|
| en.json | `"strong": "Strong matchups",` | `"weak": "Weak matchups"` |
| de.json | `"strong": "Starke Matchups",` | `"weak": "Schwache Matchups"` |
| es.json | `"strong": "Matchups favorables",` | `"weak": "Matchups desfavorables"` |
| fr.json | `"strong": "Matchups forts",` | `"weak": "Matchups faibles"` |
| it.json | `"strong": "Matchup favorevoli",` | `"weak": "Matchup sfavorevoli"` |
| pt.json | `"strong": "Matchups favoráveis",` | `"weak": "Matchups desfavoráveis"` |

Also remove the trailing comma from the `"disadvantage"` line in each file when making that edit.

After editing each file, verify it is valid JSON by running:
```bash
node -e "JSON.parse(require('fs').readFileSync('src/assets/i18n/LOCALE.json', 'utf8')); console.log('OK')"
```
(replace LOCALE with each locale name)
  </action>

  <acceptance_criteria>
Run these commands — ALL must return ZERO matches (no `strong` or `weak` keys in typeAdvantage):

```bash
# No "strong" key in typeAdvantage across all locale files
grep -n '"strong"' src/assets/i18n/en.json src/assets/i18n/de.json src/assets/i18n/es.json src/assets/i18n/fr.json src/assets/i18n/it.json src/assets/i18n/pt.json

# No "weak" key in typeAdvantage across all locale files
grep -n '"weak"' src/assets/i18n/en.json src/assets/i18n/de.json src/assets/i18n/es.json src/assets/i18n/fr.json src/assets/i18n/it.json src/assets/i18n/pt.json
```

Run these commands — ALL must return exactly 6 matches (one per locale file):

```bash
# "overwhelming" key still present in all 6 files
grep -l '"overwhelming"' src/assets/i18n/*.json | wc -l
# expected output: 6

# "advantage" key still present in all 6 files
grep -l '"advantage"' src/assets/i18n/*.json | wc -l
# expected output: 6

# "disadvantage" key still present in all 6 files
grep -l '"disadvantage"' src/assets/i18n/*.json | wc -l
# expected output: 6
```

Validate JSON is not broken in all 6 files:
```bash
for f in src/assets/i18n/*.json; do
  node -e "JSON.parse(require('fs').readFileSync('$f', 'utf8')); console.log('OK: $f')"
done
```
All 6 must print `OK: <filename>` with no errors.
  </acceptance_criteria>

  <done>
All 6 locale files have a `typeAdvantage` object containing exactly 3 keys: `overwhelming`, `advantage`, `disadvantage`. The `strong` and `weak` keys are absent. All 6 files are valid JSON.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan performs only deletions — no new data flows or trust boundaries are introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | JSON locale files | accept | Locale files are static build assets, no runtime write access; invalid JSON caught at build time by the validation step in Task 3 |
| T-07-01-02 | Denial of Service | Angular template compiler | mitigate | TypeScript compilation (`npm run build`) in Plan 07-02 Task 2 catches any dangling ViewChild references or template binding errors before deployment |
</threat_model>

<verification>
After all 3 tasks complete, run:
```bash
grep -rn "typeAdvantageModal\|queueTypeAdvantageModal" \
  src/app/main-game/roulette-container/roulettes/gym-battle-roulette/ \
  src/app/main-game/roulette-container/roulettes/elite-four-battle-roulette/
```
Expected: zero results.

```bash
grep -n '"strong"\|"weak"' src/assets/i18n/*.json
```
Expected: zero results (note: this grep pattern may match other i18n keys in unrelated contexts — inspect any results to confirm they are NOT inside a `typeAdvantage` object).

Full build verification is the responsibility of Plan 07-02.
</verification>

<success_criteria>
- `grep -rn "typeAdvantageModal" src/app/main-game/roulette-container/roulettes/` returns zero results across all 4 files.
- `grep -rn "queueTypeAdvantageModal" src/app/main-game/roulette-container/roulettes/` returns zero results.
- Both `gymLeaderPresentationModal` and `eliteFourPresentationModal` templates exist in their respective HTML files.
- `for f in src/assets/i18n/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'));console.log('OK')" || echo "FAIL: $f"; done` — all 6 print OK.
- `grep '"strong"\|"weak"' src/assets/i18n/*.json` — zero results inside typeAdvantage blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation/07-01-SUMMARY.md` using the standard summary template.
</output>
